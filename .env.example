# Application
PORT=8080
ENV=development

# JWT (required - use a strong secret in production)
JWT_SECRET=your_super_secret_key_change_me_min_32_chars
JWT_ISSUER=stm-api
JWT_EXPIRY_MINUTES=15
REFRESH_EXPIRY_DAYS=7

# Database (SQLite file path)
DB_PATH=./data/tasks.db
